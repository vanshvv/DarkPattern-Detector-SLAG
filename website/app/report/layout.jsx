// The report page is a client component, so its metadata lives here.
export const metadata = {
  title: "Report a dark pattern",
  description:
    "Report deceptive design you have run into. Reports build the public record and feed the SLAG detector's training data.",
}

export default function ReportLayout({ children }) {
  return children
}
